<%= form_with(model: mainpage, local: true) do |form| %>
  <% if mainpage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mainpage.errors.count, "error") %> prohibited this mainpage from being saved:</h2>

      <ul>
      <% mainpage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
